# TRAIN-only saved-search ambiguity audit

Use only already sealed blind tau-zero TRAIN outputs for the first and second
TRAIN groups at views 1, 6, and 16. Do not rerun a position model, access TEST,
or use reference coordinates/errors in diagnostic construction or thresholds.

Replay the published beam-width-three retention rule from each saved trace.
For the final retained beam report objective gaps from best, geographic
separations, and whether all three slots survive. Also report Sacramento/Reno
selected-coordinate separation and the fraction of common tracks assigned the
same selected candidate ID (denominator: tracks retained in both starts). Define no pass/fail threshold: these are continuous
response-free diagnostics for comparing duration and group behavior.

Bind every input and tool hash. Retain all available final beam basins. State
that beam pruning and the evaluated grid can hide modes, objective margins are
not calibrated probabilities, and fixed per-point candidate minimization can
change associations across nearby coordinates.
