#!/usr/bin/env bash
set -euo pipefail

repo_root=$(cd -- "$(dirname -- "$0")" && pwd -P)
python_bin="$repo_root/.venv/bin/python"
if [[ ! -x "$python_bin" ]]; then
  python_bin=/usr/bin/python3
fi
exec "$python_bin" "$repo_root/tools/ops.py" "$@"
